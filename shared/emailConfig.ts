/**
 * Centralized email configuration
 * Update these values to change email addresses throughout the app
 */

// Support/contact email displayed to users
export const SUPPORT_EMAIL = "hello@seriouspeople.app";

// Internal email where inbound webhook messages are forwarded
export const WEBHOOK_FORWARD_EMAIL = "hello@seriouspeople.app";

// Fallback email used by Resend when no verified domain is configured
export const RESEND_FALLBACK_EMAIL = "onboarding@resend.dev";
